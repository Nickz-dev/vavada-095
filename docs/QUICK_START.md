# ⚡ БЫСТРЫЙ СТАРТ С ШАБЛОНА

Пошаговая инструкция для запуска нового казино-проекта из шаблона.

**Время:** ~2-3 часа  
**Сложность:** ⭐⭐☆☆☆

---

## 📋 ПРЕДВАРИТЕЛЬНАЯ ПОДГОТОВКА

### Что нужно иметь:
- ✅ Дизайн нового проекта (Figma, PSD, референс)
- ✅ Список игр/контента для сайта
- ✅ Тексты для переводов (EN, RU, DE)
- ✅ OG изображение (1200x630px)
- ✅ Favicon
- ✅ Google Analytics ID (опционально)

---

## 🚀 ШАГ 1: Клонирование и настройка проекта (15 мин)

```bash
# 1. Скопировать шаблон
cp -r template-casino-base new-casino-project
cd new-casino-project

# 2. Обновить package.json
# Изменить: name, version, description

# 3. Установить зависимости
npm install

# 4. Создать .env.local
cp .env.example .env.local
```

**Обновить `.env.local`:**
```env
NEXT_PUBLIC_SITE_URL=https://newsite.com
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX  # опционально
```

**Проверка:**
- [ ] `npm run dev` работает
- [ ] Сайт открывается на `http://localhost:3000`
- [ ] Все локали работают: `/en`, `/ru`, `/de`

---

## 🔍 ШАГ 2: SEO метаданные (30 мин)

### 2.1 Обновить `components/SEO.tsx`

Найти и заменить:
```typescript
// Найти
const seoData = {
  en: {
    title: "Coins Game Casino - Online Casino",
    description: "...",
    keywords: "...",
  },
  // ...
}

// Заменить на название вашего сайта
const seoData = {
  en: {
    title: "{{YOUR_SITE_NAME}} - Online Casino",
    description: "{{YOUR_DESCRIPTION}}",
    keywords: "{{YOUR_KEYWORDS}}",
  },
  // ...
}
```

**Проверка:**
- [ ] Все локали обновлены (en, ru, de)
- [ ] Title, Description, Keywords для каждой локали
- [ ] OG метаданные обновлены

### 2.2 Обновить `app/[locale]/layout.tsx`

Найти `Organization` Schema и обновить:
```typescript
{
  "@type": "Organization",
  "name": "{{YOUR_SITE_NAME}}",
  "url": "https://{{YOUR_DOMAIN}}",
  "logo": "https://{{YOUR_DOMAIN}}/assets/icons/logo.svg",
  // ...
}
```

**Проверка:**
- [ ] Organization Schema обновлен
- [ ] WebSite Schema обновлен (в StructuredData.tsx)

### 2.3 Обновить `app/sitemap.ts`

Обновить URL в `generateMetadata`:
```typescript
metadata: {
  sitemaps: [`https://{{YOUR_DOMAIN}}/sitemap.xml`],
  // ...
}
```

### 2.4 Обновить `public/robots.txt`

```txt
Sitemap: https://{{YOUR_DOMAIN}}/sitemap.xml
```

### 2.5 Заменить OG изображение

- [ ] Заменить `public/og-image.jpg` на свой (1200x630px)
- [ ] Проверить, что файл существует

**Проверка:**
- [ ] Все SEO метаданные обновлены
- [ ] OG изображение заменено
- [ ] Sitemap URL обновлен

---

## 🌍 ШАГ 3: Переводы (45 мин)

### 3.1 Обновить `messages/en.json`

Базовая структура (обновить значения):
```json
{
  "common": {
    "login": "Login",
    "register": "Register",
    // ...
  },
  "header": {
    "login": "Login",
    "register": "Register",
    "review": "Review"
  },
  "seo": {
    "home": {
      "title": "{{YOUR_SITE_NAME}} - Online Casino",
      "description": "{{YOUR_DESCRIPTION}}"
    }
  }
}
```

### 3.2 Обновить `messages/ru.json`

- [ ] Перевести все ключи с EN на RU
- [ ] Сохранить структуру JSON

### 3.3 Обновить `messages/de.json` (если нужен)

- [ ] Перевести все ключи с EN на DE
- [ ] Сохранить структуру JSON

**Проверка:**
- [ ] Все ключи переведены
- [ ] JSON валидный (нет ошибок синтаксиса)
- [ ] Все компоненты используют правильные ключи

---

## 🎨 ШАГ 4: Создание контента (60-90 мин)

### 4.1 Обновить `components/sections/Hero.tsx`

- [ ] Создать Hero секцию по дизайну
- [ ] Добавить тексты из переводов
- [ ] Добавить изображения в `public/assets/images/`

### 4.2 Создать игровые секции

Используя `GameCarousel` компонент, создать:
- [ ] `TrendingGames.tsx` (Trending игры)
- [ ] `BestGames.tsx` (Лучшие игры)
- [ ] `CasualGames.tsx` (Casual игры)
- [ ] `LiveGames.tsx` (Live Dealer)
- [ ] `NewGames.tsx` (Новые игры)

**Пример структуры:**
```typescript
import GameCarousel from '@/components/ui/GameCarousel';

export default function TrendingGames() {
  const games = [
    { id: 1, name: "Game 1", image: "/assets/images/games/game1.jpg" },
    // ...
  ];

  return (
    <GameCarousel
      title="Trending Games"
      icon="/assets/icons/trending.svg"
      games={games}
      viewAllLink="/games/trending"
    />
  );
}
```

### 4.3 Создать другие секции

- [ ] `BigWins.tsx` (Большие выигрыши)
- [ ] `Navigation.tsx` (Навигация категорий)
- [ ] `Collections.tsx` (Коллекции игр)
- [ ] `Tournaments.tsx` (Турниры, если нужны)
- [ ] `ReviewSection.tsx` (Секция Review)

### 4.4 Обновить главную страницу

Обновить `app/[locale]/page.tsx`:
```typescript
import Hero from '@/components/sections/Hero';
import TrendingGames from '@/components/sections/TrendingGames';
// ... другие импорты

export default function Home() {
  return (
    <>
      <Header />
      <main>
        <Hero />
        <TrendingGames />
        <BestGames />
        {/* ... другие секции */}
      </main>
      <Footer />
    </>
  );
}
```

**Проверка:**
- [ ] Все секции созданы и работают
- [ ] Изображения добавлены в правильные папки
- [ ] Все ссылки работают

---

## 🔗 ШАГ 5: Настройка редиректов (15 мин)

### 5.1 Обновить `app/[locale]/redirect/page.tsx`

Найти `redirectMap` и обновить:
```typescript
const redirectMap: Record<string, string> = {
  'casino': 'https://{{YOUR_REDIRECT_URL}}/casino',
  'sport': 'https://{{YOUR_REDIRECT_URL}}/sport',
  // ...
};
```

**Проверка:**
- [ ] Все редиректы ведут на правильные URL
- [ ] Модалки показываются перед редиректами

---

## 🧪 ШАГ 6: Тестирование и финализация (30 мин)

### 6.1 Проверка локалей

- [ ] Переключение языков работает (EN, RU, DE)
- [ ] Все тексты переводятся корректно
- [ ] URL локалей корректные (`/en`, `/ru`, `/de`)

### 6.2 Проверка ссылок

- [ ] Все внутренние ссылки работают
- [ ] Редиректы работают (с модалками)
- [ ] Якорные ссылки работают (`#register`, `#login`)

### 6.3 Проверка SEO

- [ ] Meta теги обновлены (проверить через DevTools)
- [ ] Structured Data валидны (Google Rich Results Test)
- [ ] Sitemap доступен: `/sitemap.xml`
- [ ] Robots.txt доступен: `/robots.txt`

### 6.4 Проверка сборки

```bash
# Build проекта
npm run build

# Проверить ошибки
npm run lint
```

**Проверка:**
- [ ] Build проходит без ошибок
- [ ] Нет TypeScript ошибок
- [ ] Нет ESLint предупреждений (критических)

### 6.5 Финальные проверки

- [ ] Favicon заменен (`public/assets/icons/favicon.svg`)
- [ ] OG изображение заменено (`public/og-image.jpg`)
- [ ] Google Analytics настроен (если используется)
- [ ] README.md обновлен с информацией о проекте

---

## ✅ ФИНАЛЬНЫЙ ЧЕКЛИСТ

### Обязательные задачи:
- [ ] ✅ Все SEO метаданные обновлены
- [ ] ✅ Все переводы обновлены
- [ ] ✅ Контент создан (Hero, секции)
- [ ] ✅ Изображения добавлены
- [ ] ✅ Редиректы настроены
- [ ] ✅ Build проходит успешно
- [ ] ✅ Все ссылки работают

### Опциональные задачи:
- [ ] ⭕ Google Analytics настроен
- [ ] ⭕ Дополнительные SEO-страницы созданы
- [ ] ⭕ Performance оптимизирован

---

## 🚀 ГОТОВО К ДЕПЛОЮ!

После завершения всех шагов проект готов к деплою.

**Следующие шаги:**
1. Задеплоить проект (Vercel, Netlify, и т.д.)
2. Настроить Google Search Console
3. Отправить sitemap в Search Console
4. Проверить индексацию

**Время работы:** ~2-3 часа  
**Результат:** Полностью готовый проект с SEO оптимизацией!

---

## 📞 НУЖНА ПОМОЩЬ?

Если что-то не работает:
1. Проверьте документацию в `docs/`
2. Сравните с работающим проектом
3. Проверьте консоль браузера и терминала

