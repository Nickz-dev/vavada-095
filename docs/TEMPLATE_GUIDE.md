# 📋 Руководство по созданию шаблона и быстрому старту новых проектов

**Цель:** Создать базовый шаблон для быстрого запуска похожих казино-проектов.

**Время разработки с нуля:** ~12 часов  
**Время разработки с шаблона:** ~2-3 часа (оценка)

---

## 🎯 СТРАТЕГИЯ: Базовый шаблон vs Копирование проекта

### ✅ Рекомендуется: Создать базовый шаблон

**Преимущества:**
- ✅ Минимум конфигурации для старта
- ✅ Чистая структура без лишнего
- ✅ Готовые SEO-компоненты
- ✅ Настроенная мультиязычность
- ✅ Документация по быстрому старту

**Что оставить в шаблоне:**
1. **Базовая структура Next.js** (App Router, TypeScript, Tailwind)
2. **SEO-инфраструктура** (все компоненты Structured Data, SEO, sitemap, robots.txt)
3. **Мультиязычность** (next-intl, middleware, базовая структура JSON)
4. **Базовые компоненты** (Header, Footer, Modal, AnchorLink)
5. **Утилиты** (GoogleAnalytics, redirect система)
6. **Пустые компоненты-заглушки** для быстрого старта

**Что убрать из шаблона:**
1. Специфичный контент (Hero, BigWins, все GameCarousel инстансы)
2. Конкретные изображения (оставить только структуру папок)
3. Конкретные переводы (оставить только базовую структуру JSON)
4. Review страницу (можно оставить как пример)

---

## 📦 СТРУКТУРА БАЗОВОГО ШАБЛОНА

### 1. Корневые файлы
```
template-casino-base/
├── .env.example              # Шаблон переменных окружения
├── .gitignore
├── next.config.js
├── package.json              # Базовые зависимости
├── tailwind.config.ts
├── tsconfig.json
├── middleware.ts             # Готовый middleware для локалей
├── README.md                 # Инструкция по быстрому старту
└── docs/
    ├── TEMPLATE_GUIDE.md     # Этот файл
    ├── QUICK_START.md        # Пошаговая инструкция
    └── SEO_SETUP.md          # Настройка SEO компонентов
```

### 2. Структура app/
```
app/
├── [locale]/
│   ├── layout.tsx            # Базовый layout с SEO
│   ├── page.tsx              # Минимальная главная (Header, Footer)
│   └── redirect/
│       └── page.tsx          # Система редиректов
├── globals.css               # Tailwind стили
├── sitemap.ts                # Динамический sitemap
└── robots.ts                 # Robots.txt генератор
```

### 3. Структура components/
```
components/
├── SEO/                      # SEO инфраструктура
│   ├── SEO.tsx               # Базовый SEO компонент
│   ├── StructuredData.tsx    # Organization, WebSite
│   ├── ArticleStructuredData.tsx
│   ├── BreadcrumbStructuredData.tsx
│   └── FAQSchema.tsx
├── layout/                   # Базовые layout компоненты
│   ├── Header.tsx            # Базовый Header (заглушка)
│   └── Footer.tsx            # Базовый Footer (заглушка)
├── ui/                       # UI компоненты
│   ├── Modal.tsx             # Готовый Modal
│   ├── AnchorLink.tsx        # Готовый AnchorLink
│   ├── LanguageSwitcher.tsx  # Готовый LanguageSwitcher
│   └── GameCarousel.tsx      # Базовый GameCarousel (заглушка)
├── sections/                 # Секции страницы (заглушки)
│   ├── Hero.tsx              # Заглушка Hero
│   ├── GameSection.tsx       # Заглушка игровой секции
│   └── ReviewSection.tsx     # Заглушка Review секции
└── GoogleAnalytics.tsx       # Готовый GA компонент
```

### 4. Структура messages/
```
messages/
├── en.json                   # Базовая структура
├── ru.json                   # Базовая структура
└── de.json                   # Базовая структура (опционально)
```

**Базовая структура JSON:**
```json
{
  "common": {
    "login": "Login",
    "register": "Register",
    "language": "Language",
    "cancel": "Cancel",
    "continue": "Continue"
  },
  "header": {
    "login": "Login",
    "register": "Register",
    "review": "Review"
  },
  "footer": {
    "copyright": "© 2025"
  },
  "seo": {
    "home": {
      "title": "{{siteName}} - Online Casino",
      "description": "Play at {{siteName}}"
    }
  }
}
```

### 5. Структура public/
```
public/
├── robots.txt                # Базовый robots.txt
├── og-image.jpg              # Placeholder OG изображение
└── assets/
    ├── icons/
    │   ├── favicon.svg       # Базовый favicon
    │   └── ...
    └── images/
        └── .gitkeep          # Структура для изображений
```

---

## 🚀 БЫСТРЫЙ СТАРТ С ШАБЛОНА (Чеклист)

### Шаг 1: Клонирование и базовая настройка (15 мин)
- [ ] Скопировать шаблон в новый проект
- [ ] Изменить `package.json` (name, version)
- [ ] Настроить `.env.local`:
  ```env
  NEXT_PUBLIC_SITE_URL=https://newsite.com
  NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
  ```
- [ ] Установить зависимости: `npm install`

### Шаг 2: Обновление SEO метаданных (30 мин)
- [ ] Обновить `components/SEO.tsx` (название сайта, описание)
- [ ] Обновить `app/[locale]/layout.tsx` (Organization Schema)
- [ ] Обновить `app/sitemap.ts` (название сайта в URL)
- [ ] Обновить `public/robots.txt` (Sitemap URL)
- [ ] Заменить `public/og-image.jpg` на свой OG изображение

### Шаг 3: Обновление переводов (45 мин)
- [ ] Обновить `messages/en.json` (название, тексты)
- [ ] Обновить `messages/ru.json` (переводы)
- [ ] Обновить `messages/de.json` (переводы, если нужен)
- [ ] Проверить все ключи используются в компонентах

### Шаг 4: Создание контента (60-90 мин)
- [ ] Создать Hero компонент (дизайн + контент)
- [ ] Создать игровые секции (GameCarousel инстансы)
- [ ] Создать другие секции (BigWins, Navigation, Collections и т.д.)
- [ ] Добавить изображения в `public/assets/images/`

### Шаг 5: Настройка редиректов (15 мин)
- [ ] Обновить `app/[locale]/redirect/page.tsx` (redirectMap)
- [ ] Обновить URL редиректа в компонентах

### Шаг 6: Тестирование и финализация (30 мин)
- [ ] Проверить все локали работают
- [ ] Проверить все ссылки работают
- [ ] Проверить build: `npm run build`
- [ ] Проверить SEO компоненты
- [ ] Обновить `README.md` с информацией о проекте

**Итого: ~3 часа работы**

---

## 📝 ЧТО МЕНЯТЬ В КАЖДОМ НОВОМ ПРОЕКТЕ

### Обязательные изменения:
1. **Название проекта** (`package.json`, `README.md`)
2. **SEO метаданные** (`components/SEO.tsx`, `app/[locale]/layout.tsx`)
3. **URL сайта** (`.env.local`, `sitemap.ts`, `robots.txt`)
4. **Переводы** (`messages/*.json`)
5. **OG изображение** (`public/og-image.jpg`)
6. **Favicon** (`public/assets/icons/favicon.svg`)
7. **Контент компонентов** (Hero, секции, игры)
8. **Редиректы** (`app/[locale]/redirect/page.tsx`)

### Опциональные изменения:
- Добавить/убрать локали (en, ru, de)
- Добавить новые секции страницы
- Настроить Google Analytics
- Добавить новые SEO-страницы

---

## 🔧 КОМАНДЫ ДЛЯ БЫСТРОГО СТАРТА

```bash
# 1. Клонировать шаблон
cp -r template-casino-base new-casino-project
cd new-casino-project

# 2. Установить зависимости
npm install

# 3. Настроить переменные окружения
cp .env.example .env.local
# Отредактировать .env.local

# 4. Запустить dev сервер
npm run dev

# 5. Проверить build
npm run build

# 6. Запустить production
npm start
```

---

## 💡 РЕКОМЕНДАЦИИ ПО ИСПОЛЬЗОВАНИЮ ШАБЛОНА

### ✅ Делать:
- Использовать шаблон как базу для всех похожих проектов
- Хранить шаблон в отдельном репозитории
- Документировать изменения в шаблоне
- Обновлять шаблон после обнаружения багов/улучшений

### ❌ Не делать:
- Не хранить специфичный контент в шаблоне
- Не коммитить `.env.local` файлы
- Не добавлять временные решения в шаблон
- Не удалять SEO-инфраструктуру из шаблона

---

## 📊 ОЦЕНКА ЭКОНОМИИ ВРЕМЕНИ

### Без шаблона:
- Настройка Next.js + TypeScript + Tailwind: ~1 час
- Настройка i18n (next-intl): ~1 час
- SEO инфраструктура (Structured Data, sitemap, robots): ~3 часа
- Базовые компоненты (Header, Footer, Modal): ~2 часа
- Система редиректов: ~1 час
- Google Analytics: ~30 мин
- **Итого: ~8.5 часов инфраструктуры**

### С шаблоном:
- Клонирование и базовая настройка: ~15 мин
- Обновление SEO метаданных: ~30 мин
- Обновление переводов: ~45 мин
- Создание контента: ~60-90 мин (зависит от дизайна)
- Настройка редиректов: ~15 мин
- **Итого: ~2.5-3 часа всего**

### Экономия времени: **~5-6 часов на проект!**

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Создать базовый шаблон:**
   - Скопировать текущий проект
   - Удалить специфичный контент
   - Оставить только инфраструктуру
   - Документировать структуру

2. **Тестировать шаблон:**
   - Использовать для следующего проекта
   - Зафиксировать время разработки
   - Собрать обратную связь

3. **Итеративно улучшать:**
   - Добавлять полезные компоненты
   - Улучшать документацию
   - Оптимизировать процесс

---

**Время создания шаблона:** ~2-3 часа  
**Экономия на каждом проекте:** ~5-6 часов  
**ROI:** Окупается уже после первого проекта!

