# Структура проекта Coins Game Casino

## Архитектура проекта

Проект построен на Next.js 14 с App Router, TypeScript, Tailwind CSS и next-intl для интернационализации.

## Основные компоненты

### 1. Интернационализация (i18n)
- **Локали**: 20 языков (EN, RU, DE, PL, TR, AZ, BG, EL, ES-MX, FI, FR-CA, HI, HU, IT, JA, KK, KO, PT-BR, SV, UZ)
- **Конфигурация**: `i18n/request.ts`, `i18n/config.ts`
- **Переводы**: `messages/*.json` (20 файлов)
- **Middleware**: `middleware.ts` (обработка локалей, отключено автоопределение)

### 2. SEO и редиректы
- **AnchorLink**: `components/AnchorLink.tsx` - компонент якорной ссылки с модалкой
- **Modal**: `components/Modal.tsx` - модалка для рекомендаций
- **Redirect**: `app/[locale]/redirect/page.tsx` - скрытая страница редиректов
- **Robots.txt**: `public/robots.txt` - запрет индексации redirect раздела

### 3. Страницы
- **Главная**: `app/[locale]/page.tsx` ✅ Завершена
- **Обзор**: `app/[locale]/review/page.tsx`
- **Layout**: `app/[locale]/layout.tsx`
- **Redirect**: `app/[locale]/redirect/page.tsx`

## Структура файлов

```
coinsgamecasino/
├── app/
│   ├── [locale]/
│   │   ├── layout.tsx          # Layout с локализацией
│   │   ├── page.tsx            # Главная страница
│   │   └── redirect/
│   │       └── page.tsx        # Скрытая страница редиректов
│   └── globals.css             # Глобальные стили
├── components/
│   ├── layout/
│   │   ├── Header.tsx          # Шапка сайта
│   │   ├── Footer.tsx          # Футер сайта
│   │   └── BeamCategories.tsx  # Навигация по категориям
│   ├── sections/
│   │   ├── Hero.tsx            # Карусель промо-слайдов
│   │   ├── GamesSection.tsx    # Секция с играми и фильтрами
│   │   └── ReviewSeoSection.tsx # SEO-секция обзора
│   ├── ui/
│   │   ├── GameCard.tsx        # Карточка игры
│   │   ├── GameFilters.tsx     # Фильтры игр
│   │   ├── GameBadge.tsx       # Бейджи (HIT/NEW/PRE)
│   │   ├── GameSortFlag.tsx    # Флаги фильтров
│   │   ├── MoreGamesButton.tsx # Кнопка "Больше игр"
│   │   ├── Modal.tsx           # Компонент модалки
│   │   ├── LanguageSwitcher.tsx # Переключатель языков
│   │   └── AnchorLink.tsx       # Компонент якорной ссылки
│   └── SEO/
│       ├── SEO.tsx              # SEO метаданные
│       └── StructuredData.tsx   # Структурированные данные
├── i18n/
│   ├── config.ts               # Конфигурация локалей
│   └── request.ts              # Конфигурация next-intl
├── messages/
│   ├── en.json                 # Английские переводы
│   ├── ru.json                 # Русские переводы
│   └── ...                     # 18 дополнительных локалей
├── public/
│   └── robots.txt              # SEO конфигурация
├── docs/
│   ├── PROJECT_STRUCTURE.md     # Эта документация
│   ├── HOME_PAGE_AUDIT.md      # Аудит главной страницы
│   ├── TEMPLATE_GUIDE.md       # Руководство по шаблону
│   ├── QUICK_START.md          # Быстрый старт
│   └── SEARCH_ENGINE_OPTIMIZATION.md  # SEO документация
├── middleware.ts               # Next.js middleware для i18n
├── next.config.js              # Конфигурация Next.js
├── tailwind.config.ts          # Конфигурация Tailwind
└── package.json                # Зависимости проекта
```

## Использование компонентов

### AnchorLink - Якорная ссылка с редиректом

```tsx
import AnchorLink from '@/components/AnchorLink';

<AnchorLink
  href="#casino"              // Якорная ссылка для SEO
  anchorId="casino"           // ID секции на странице
  redirectUrl="https://example.com/casino"  // URL для редиректа
  redirectType="casino"       // Тип редиректа
  refParam="hero-cta"         // Референс для аналитики
  showModal={true}            // Показывать модалку
  className="btn-primary"
>
  Go to Casino
</AnchorLink>
```

### Modal - Модалка

```tsx
import Modal from '@/components/Modal';

<Modal
  isOpen={isOpen}
  onClose={() => setIsOpen(false)}
  title="Attention"
>
  <p>Modal content here</p>
</Modal>
```

## Ключевые особенности

1. **SEO-оптимизация**: Все ссылки валидные для поисковых систем
2. **Скрытые редиректы**: Через `/redirect/` раздел (закрыт в robots.txt)
3. **Модалки**: Показываются перед редиректом с рекомендациями
4. **Интернационализация**: Поддержка EN и RU через next-intl

## Для создания нового сайта на этой структуре

1. Скопировать всю структуру проекта
2. Обновить переводы в `messages/en.json` и `messages/ru.json`
3. Изменить контент на главной странице `app/[locale]/page.tsx`
4. Обновить `robots.txt` с правильным доменом
5. Настроить редиректы в `app/[locale]/redirect/page.tsx` (redirectMap)

## Зависимости

Основные пакеты:
- `next@^14.2.0`
- `react@^18.3.0`
- `next-intl@^3.19.0` (установлена версия 3.26.5)
- `tailwindcss@^3.4.0`
- `typescript@^5.5.0`
- `react-icons` - иконки для категорий и UI

## Статус разработки

### ✅ Завершено
- Главная страница со всеми компонентами
- 40 слотов в каталоге
- Полная локализация (20 языков)
- Адаптивный дизайн
- Система фильтрации игр
- SEO-секция обзора

### ⏭️ В разработке
- Страница обзора (`/review`)

### 📋 Планируется
- Дополнительные страницы
- Расширение функционала

