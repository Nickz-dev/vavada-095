# 🎰 Casino Template - Базовый шаблон для казино-проектов

Базовый шаблон для быстрого запуска новых казино-проектов на Next.js 14.

**Время разработки с шаблона:** ~2-3 часа  
**Время разработки с нуля:** ~12 часов

## ✨ Особенности

- ✅ Next.js 14 с App Router
- ✅ TypeScript
- ✅ Tailwind CSS
- ✅ Интернационализация (EN/RU) с next-intl
- ✅ Полная SEO-оптимизация (мета-теги, Open Graph, Twitter Cards, JSON-LD)
- ✅ Динамический sitemap.xml с поддержкой локалей
- ✅ Robots.txt с правильными настройками
- ✅ Система скрытых редиректов через модалки
- ✅ SEO-оптимизированные якорные ссылки
- ✅ Готовые компоненты (Header, Footer, Modal, AnchorLink, LanguageSwitcher)
- ✅ Google Analytics поддержка

## 🚀 Быстрый старт

См. подробную инструкцию в [`docs/QUICK_START.md`](docs/QUICK_START.md)

### 1. Клонирование и настройка

```bash
# Скопировать шаблон
cp -r template-casino-base new-casino-project
cd new-casino-project

# Установить зависимости
npm install

# Создать .env.local
cp .env.example .env.local
# Отредактировать .env.local с вашими данными
```

### 2. Обновить конфигурацию

1. Обновить SEO метаданные в `components/SEO/SEO.tsx`
2. Обновить переводы в `messages/en.json` и `messages/ru.json`
3. Обновить URL в `app/sitemap.ts` и `public/robots.txt`
4. Настроить редиректы в `app/[locale]/redirect/page.tsx`

### 3. Создать контент

1. Создать Hero секцию в `components/sections/Hero.tsx`
2. Создать игровые секции используя `GameCarousel`
3. Добавить изображения в `public/assets/images/`

## 📦 Структура проекта

```
template-casino-base/
├── app/
│   ├── [locale]/          # Локализованные страницы
│   ├── sitemap.ts         # Динамический sitemap
│   └── globals.css        # Tailwind стили
├── components/
│   ├── SEO/               # SEO инфраструктура
│   ├── layout/            # Header, Footer
│   ├── ui/                # UI компоненты
│   └── sections/          # Секции страницы
├── messages/              # Переводы (en.json, ru.json)
├── public/
│   ├── assets/            # Изображения и иконки
│   └── robots.txt         # Robots.txt
└── docs/                  # Документация
```

## 📚 Документация

- [`QUICK_START.md`](docs/QUICK_START.md) - Пошаговая инструкция по быстрому старту
- [`TEMPLATE_GUIDE.md`](docs/TEMPLATE_GUIDE.md) - Руководство по использованию шаблона
- [`TEMPLATE_PREPARATION.md`](docs/TEMPLATE_PREPARATION.md) - Подготовка шаблона
- [`PROJECT_STRUCTURE.md`](docs/PROJECT_STRUCTURE.md) - Структура проекта
- [`SEARCH_ENGINE_OPTIMIZATION.md`](docs/SEARCH_ENGINE_OPTIMIZATION.md) - SEO настройка

## 🛠️ Разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Запуск production
npm start

# Линтинг
npm run lint
```

## 📝 Что нужно изменить в новом проекте

1. **Название проекта** (`package.json`, `README.md`)
2. **SEO метаданные** (`components/SEO/SEO.tsx`, `components/SEO/StructuredData.tsx`)
3. **URL сайта** (`.env.local`, `sitemap.ts`, `robots.txt`)
4. **Переводы** (`messages/*.json`)
5. **OG изображение** (`public/og-image.jpg`)
6. **Favicon** (`public/assets/icons/favicon.svg`)
7. **Контент компонентов** (Hero, секции, игры)
8. **Редиректы** (`app/[locale]/redirect/page.tsx`)

## ⚡ Экономия времени

- **Без шаблона:** ~8.5 часов инфраструктуры
- **С шаблоном:** ~2.5-3 часа всего
- **Экономия:** ~5-6 часов на проект!

## 📞 Поддержка

Если что-то не работает:
1. Проверьте документацию в `docs/`
2. Сравните с работающим проектом
3. Проверьте консоль браузера и терминала

